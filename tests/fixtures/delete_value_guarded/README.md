# Delete (value-guarded)

Demonstrates the `-` suffix with a scalar value. The key is removed only when
the base value equals the patch value.

If `author` were `"Jane Doe"`, the delete would not run.
