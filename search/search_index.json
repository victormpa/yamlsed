{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"YAMLSED","text":"<p>YAML template matching &amp; processing.</p> <p>Documentation \u2014 concepts, matching, patching, and examples.</p>"},{"location":"#getting-started","title":"Getting Started","text":"<pre><code>pip install yamlsed\n</code></pre>"},{"location":"#usage","title":"Usage","text":"<p>YAMLSED pairs a base template with a patch document. The <code>match</code> block selects which base documents to update; the <code>patch</code> block describes the changes to apply. <pre><code># base.yaml\n\nname: example\nversion: 1\napproved: false\n</code></pre></p> <p>Write your patches <pre><code># patch.yaml\nmatch:\n  - name: example\n    version: 1\n\npatch:\n  approved: true\n</code></pre></p> <p>Run progmatically: <pre><code>import yaml\n\nfrom yamlsed.patch import Patch\nfrom yamlsed.template import Template\n\nbase = Template.load(\"base.yaml\")\n\npatch = Patch.load(\"patch.yaml\")\n\nbase.apply(patch)\nprint(base)\n</code></pre></p> <p>Or from the command line: <pre><code>yamlsed base.yaml patch.yaml\n</code></pre></p> <p>Output:</p> <pre><code>name: example\nversion: 1\napproved: true\n</code></pre> <p>More examples are in Examples.</p>"},{"location":"#status","title":"Status","text":"<p>Early / work in progress. Syntax described here reflects the current design and may change.</p>"},{"location":"concepts/","title":"Concepts","text":"<p>There are two types of documents:</p> Document Purpose Base A normal YAML template \u2014 the file you want to keep updated. Patch A document containing a <code>match</code> block and a <code>patch</code> block. <p>The patch is applied to the base whenever the base satisfies the <code>match</code> selector.</p>"},{"location":"concepts/#base","title":"Base","text":"<p>The base is just ordinary YAML:</p> <pre><code>name: \"example\"\nversion: 1\ndescription: \"This is an example YAML file\"\nauthor: \"John Doe\"\nlicense: \"MIT\"\napproved: false\nscore: 9.8\ntags:\n  - \"yaml\"\n  - \"templates\"\n  - \"parser\"\nlabels:\n  operating_system: \"linux\"\n  architecture: \"x86_64\"\ndependencies:\n  - name: \"example\"\n    version: \"1.0.0\"\n</code></pre>"},{"location":"concepts/#patch","title":"Patch","text":"<p>A patch document has two blocks:</p> <pre><code>match:    # which base documents this patch applies to\n  - name: \"example\"\n    version: 1\n\npatch:    # mutations to apply\n  approved: true\n</code></pre> <p>A single <code>.patch.yaml</code> file may contain multiple patch documents separated by <code>---</code>. Each document has its own <code>match</code> and <code>patch</code> blocks. When applied, patches run top-to-bottom in file order \u2014 each patch operates on the result of the previous one.</p> <pre><code>match:\n  - name: \"example\"\npatch:\n  approved: true\n---\nmatch:\n  - name: \"example\"\npatch:\n  version: 2\n</code></pre>"},{"location":"concepts/#file-naming","title":"File naming","text":"<p>Patches must live in files named <code>{name}.patch.yaml</code>, where <code>{name}</code> is a meaningful, descriptive prefix of your choosing \u2014 for example <code>approve.patch.yaml</code> or <code>bump-version.patch.yaml</code>. The <code>.patch.yaml</code> extension is what marks the file as a patch.</p> <ul> <li><code>Patch.load()</code> requires at least one document and rejects empty documents   (stray <code>---</code> separators with no content).</li> <li><code>Patch.load()</code> rejects any file whose name does not end in <code>.patch.yaml</code>   (or that has an empty prefix, like a bare <code>.patch.yaml</code>).</li> <li>During discovery, files ending in <code>.patch.yaml</code> are loaded as <code>Patch</code>   instances, while all other <code>.yaml</code> files are loaded as <code>Template</code> instances.</li> </ul> <pre><code>templates/\n  service.yaml          # loaded as a Template\n  approve.patch.yaml    # loaded as a Patch\n</code></pre>"},{"location":"examples/","title":"Examples","text":""},{"location":"matching/","title":"Matching","text":"<p><code>match</code> is a list of selectors. The base is targeted if it matches any selector in the list (logical OR). Each selector is a set of key/value pairs that must all be satisfied on the base \u2014 keys must be present, and each value is checked according to its type.</p> <pre><code>match:\n  - name: \"example\"\n    version: 1\n  - name: \"example2\"\n    version: 1\n</code></pre> <p>Matches a base named <code>example</code> or <code>example2</code>, in either case with <code>version: 1</code>.</p>"},{"location":"matching/#wildcard","title":"Wildcard","text":"<p>Use <code>*</code> as a value to accept anything at that position:</p> <pre><code>match:\n  - name: \"*\"\n</code></pre> <p>Matches every base document, regardless of <code>name</code>.</p> <p>Wildcards work at any depth \u2014 for example <code>labels: { os: \"*\" }</code> matches any <code>os</code> label value.</p>"},{"location":"matching/#regex","title":"Regex","text":"<p>String values are matched as regular expressions (full string, via <code>re.fullmatch</code>). A plain literal like <code>\"example\"</code> still works \u2014 it simply matches that exact string. Use regex syntax when you need flexible matching:</p> <pre><code>match:\n  - name: \"Example.*?\"\n</code></pre> <p>Matches any base whose <code>name</code> starts with <code>Example</code> \u2014 for example <code>Example model</code>.</p> <p>Invalid regex patterns raise an error at apply time.</p>"},{"location":"matching/#other-types","title":"Other types","text":"<p>Scalars other than strings (integers, floats, booleans) use equality. Lists are matched positionally \u2014 same length, each element checked in order.</p>"},{"location":"tests/","title":"Testing suite","text":"<p>Each folder demonstrates one concept with four files:</p> File Purpose <code>base.yaml</code> The template to patch <code>{name}.patch.yaml</code> <code>match</code> + <code>patch</code> document (named after the example) <code>result.yaml</code> Expected output after applying the patch <code>README.md</code> What the example shows <p>The original combined reference is still at tests/fixtures/base.yaml.</p>"},{"location":"tests/#catalog","title":"Catalog","text":"Example Operation Docs section matching OR selectors Matching replace_scalar Replace scalar Scalars replace_array Replace array Array replace_object Replace object Object append_array Append to array (<code>+</code>) Array append_missing_array Append to missing array (<code>+</code>) Array delete_value_guarded Value-guarded delete Scalars delete_null_guarded Null-guarded delete Object delete_unconditional Unconditional delete (<code>*</code>) Scalars delete_array Delete entire array Object delete_exact_element Exact element match Array delete_partial_element Partial element match (<code>-?</code>) Array full_walkthrough All operations combined Patching"},{"location":"patch-operations/","title":"Patching","text":"<p>The <code>patch</code> block is keyed by the field you want to change. The key suffix selects the operation. Which operations are available depends on the type of the field being patched.</p> <p>See the subpages for operation details by type:</p> <ul> <li>Scalars \u2014 string, int, float, bool</li> <li>Arrays and objects \u2014 list and map operations</li> <li>Functions \u2014 <code>{{function(...)}}</code> expressions</li> </ul>"},{"location":"patch-operations/#suffix-reference","title":"Suffix reference","text":"Suffix Operation Value none Replace New scalar, or new array (replaces the whole array) <code>+</code> Append list = append elements; dict = merge keys into object <code>+?</code> Append (guarded) dict = merge keys only if the field exists; otherwise skip <code>-</code> Delete <code>*</code> = always; <code>null</code> = if null; a scalar = if equal; keys = exact array-element match <code>-?</code> Delete (partial) Keys to match an array element partially"},{"location":"patch-operations/collections/","title":"Arrays and objects","text":""},{"location":"patch-operations/collections/#array","title":"Array","text":"<p>Replace the whole array (no suffix):</p> <pre><code>patch:\n  tags:\n    - \"yaml\"        # replaces every existing tag\n</code></pre> <p>Append with <code>key+</code> \u2014 adds the new value(s) instead of replacing. If the field does not exist yet, Yamlsed creates it as an empty array first.</p> <pre><code>patch:\n  tags+:\n    - \"added\"       # tags now also contains \"added\"\n</code></pre> <pre><code>patch:\n  children+:\n    - name: \"baz\"   # creates `children` and appends the element\n</code></pre> <p>Delete by exact element match with <code>key-</code>. Provide keys, and an array element is removed only when it matches exactly (all specified keys equal, no extras beyond what you list):</p> <pre><code>patch:\n  dependencies-:\n    - name: \"example2\"\n      version: 1\n</code></pre> <p>Delete by partial element match with <code>key-?</code> \u2014 the listed keys must be present and equal, but the element may carry additional keys:</p> <pre><code>patch:\n  dependencies-?:\n    - name: \"example2\"\n      version: 1        # removes any dependency where name+version match\n</code></pre>"},{"location":"patch-operations/collections/#object","title":"Object","text":"<p>Replace the whole object (no suffix) \u2014 the entire map is overwritten.</p> <p>Append / merge with <code>key+</code> \u2014 merges the new keys into the existing object instead of replacing it. If the field does not exist yet, Yamlsed creates it as an empty object first.</p> <pre><code>patch:\n  labels+:\n    environment: \"production\"   # merges into `labels`, keeping existing keys\n</code></pre> <p>Nested <code>key+</code> inside a replace value works the same way. When the patch value is an array, Yamlsed finds all elements that partially match the non-suffixed keys and merges into each of them:</p> <pre><code>patch:\n  interfaces:\n    - type: database\n      config+:\n        username: admin\n        password: password\n</code></pre> <p>Every matching <code>database</code> interface keeps its existing <code>name</code> and <code>config</code> keys; <code>config+</code> adds <code>username</code> and <code>password</code> alongside <code>host</code> and <code>port</code>.</p> <p>Guarded merge with <code>key+?</code> \u2014 merges into an object only when the key already exists in the base. If the field is missing, Yamlsed skips the operation.</p> <p>Null-guarded delete with <code>key-</code> and a <code>null</code> value \u2014 deletes only when the field is null:</p> <pre><code>patch:\n  healthcheck-: null    # deletes `healthcheck` only if it is null\n</code></pre> <p>Unconditional delete with <code>*</code> \u2014 deletes regardless of the current value:</p> <pre><code>patch:\n  metadata-: \"*\"        # always deletes `metadata`\n</code></pre>"},{"location":"patch-operations/functions/","title":"Functions","text":"<p>Patch values can be expressions written as <code>{{function(...)}}</code>. They are evaluated when the patch is applied, not when it is loaded. Bare expressions are supported in patch files \u2014 <code>Patch.load()</code> quotes them so YAML can parse them:</p> <pre><code>patch:\n  updated: \"{{now('YYYY-MM-DD HH:mm:ss')}}\"\n  random: {{random(1, 100)}}\n  uppercase: {{uppercase(\"hello\")}}\n  env: {{env(\"HOME\")}}\n</code></pre>"},{"location":"patch-operations/functions/#built-in-functions","title":"Built-in functions","text":"Function Description <code>now(format)</code> Current date/time (<code>YYYY</code>, <code>MM</code>, <code>DD</code>, <code>HH</code>, <code>mm</code>, <code>ss</code> tokens) <code>random(min, max)</code> Random integer in range <code>uppercase</code>, <code>lowercase</code>, <code>capitalize</code>, <code>reverse</code>, <code>length</code>, <code>trim</code> String operations <code>substring(s, start, end)</code> Slice a string <code>replace(s, old, new)</code> Replace substring <code>split(s, sep)</code> Split into a list <code>join(items, sep)</code> Join a list into a string <code>env(name)</code> Read an environment variable <code>old()</code> Original value of the field being patched (before apply); <code>null</code> if missing <p>Function results can be chained with type casts and methods:</p> <pre><code>patch:\n  score: {{env(\"SCORE\").float().round(2)}}\n  tags: {{env(\"TAGS\").split(\",\").array()}}\n  label: {{env(\"NAME\").trim().upper()}}\n  enabled: {{env(\"FLAG\").bool()}}\n</code></pre>"},{"location":"patch-operations/functions/#type-casts-and-methods","title":"Type casts and methods","text":"<p>Coerce the current value, then call methods on the result:</p> Cast Result Methods <code>.str()</code> string <code>.split(sep)</code>, <code>.replace(old, new)</code>, <code>.trim()</code>, <code>.upper()</code>, <code>.lower()</code>, <code>.capitalize()</code>, <code>.substring(start, end)</code>, <code>.reverse()</code>, <code>.length()</code> <code>.int()</code> int <code>.round(decimals)</code>, <code>.abs()</code> <code>.float()</code> float <code>.round(decimals)</code>, <code>.abs()</code> <code>.bool()</code> bool (<code>true</code>/<code>false</code>/<code>yes</code>/<code>no</code>/<code>1</code>/<code>0</code>, case-insensitive) <code>.object()</code> dict (JSON parse if string) <code>.keys()</code>, <code>.values()</code>, <code>.get(key)</code> <code>.array()</code> list (JSON parse if string) <code>.join(sep)</code>, <code>.reverse()</code>, <code>.sort()</code>, <code>.unique()</code>, <code>.first()</code>, <code>.last()</code>, <code>.length()</code> <p>Expressions are resolved only in patch values (replace, append, merge). Match selectors and delete guards are never evaluated.</p>"},{"location":"patch-operations/scalars/","title":"Scalars","text":""},{"location":"patch-operations/scalars/#string","title":"String","text":"<p>Replace the value (no suffix):</p> <pre><code>patch:\n  name: \"foo\"       # example -&gt; foo\n</code></pre> <p>Delete with a guard (<code>key-</code>), or unconditionally with <code>*</code>:</p> <pre><code>patch:\n  author-: \"John Doe\"   # deletes `author` only if it equals \"John Doe\"\n  license-: *           # always deletes `license`\n</code></pre>"},{"location":"patch-operations/scalars/#int","title":"Int","text":"<p>Replace the value (no suffix):</p> <pre><code>patch:\n  version: 2        # 1 -&gt; 2\n</code></pre> <p>Delete with a guard (<code>key-</code>), or unconditionally with <code>*</code>:</p> <pre><code>patch:\n  version-: 1       # deletes `version` only if it equals 1\n  version-: *       # always deletes `version`\n</code></pre>"},{"location":"patch-operations/scalars/#float","title":"Float","text":"<p>Replace the value (no suffix):</p> <pre><code>patch:\n  score: 10.0       # 9.8 -&gt; 10.0\n</code></pre> <p>Delete with a guard (<code>key-</code>), or unconditionally with <code>*</code>:</p> <pre><code>patch:\n  score-: 9.8       # deletes `score` only if it equals 9.8\n  score-: *         # always deletes `score`\n</code></pre>"},{"location":"patch-operations/scalars/#bool","title":"Bool","text":"<p>Replace the value (no suffix):</p> <pre><code>patch:\n  approved: true    # false -&gt; true\n</code></pre> <p>Delete with a guard (<code>key-</code>), or unconditionally with <code>*</code>:</p> <pre><code>patch:\n  approved-: false  # deletes `approved` only if it equals false\n  approved-: *      # always deletes `approved`\n</code></pre>"}]}